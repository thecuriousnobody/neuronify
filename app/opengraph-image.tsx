// Generated on-demand (and cached) rather than prerendered at build, so a
// font fetch can never block the build.
export const dynamic = 'force-dynamic';
export { default, size, contentType, alt } from '@/lib/og';
